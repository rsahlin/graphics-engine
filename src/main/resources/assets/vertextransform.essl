/* Copyright 2017 Richard Sahlin

 * Vertex shader for normal vertex transform.
 * @author Richard Sahlin
 */
#version 100

//Put array declaration after name for GLSL compatibility
uniform mat4 uMVMatrix;
uniform mat4 uProjectionMatrix;

attribute vec3 aPosition; //Position of vertex, offset from matrix
attribute vec4 aColor;

varying vec4 color;

void main() {
    vec4 pos = uMVMatrix * vec4(aPosition, 1.0);
    gl_Position = uProjectionMatrix * pos;
    color = aColor;
    
}
